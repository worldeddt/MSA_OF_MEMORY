# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: ✅Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      - name: ✨패키징
        working-directory: ./
        run: |
          zip -r msa_of_memory.zip ./api-gateway ./eureka_server ./springKafka ./user-service
      - name: 🌎 AWS에 접속
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

#      - name: 🦖 S3에 압축된 서버 코드를 업로드
#        run: aws s3 cp --region ap-northeast-2 ./partners.zip s3://partners-github-test/deploy/
#
#      - name: 🚀 AWS codeDeploy로 배포를 시작
#        run: aws deploy create-deployment
#          --application-name partners-erp-code-deploy-test
#          --deployment-config-name CodeDeployDefault.OneAtATime
#          --deployment-group-name partners-erp-deploy-test-group
#          --s3-location bucket=partners-github-test,bundleType=zip,key=deploy/msa_of_memory.zip
#      - name: ✨eureka server build
#        working-directory: ./eureka_server
#        run: |
#          zip -r msa_of_memory.zip ./apigate-service
#          ./eureka_server/cloud-discovery-eureka-server/mvnw clean package -DskipTests
#      - name: ✅copy the eureka server
#        working-directory: ./eureka_server/cloud-discovery-eureka-server/
#        run: |
#          mv target/*.jar cloud-discovery-eureka-server.jar
#      - name: 📦 빌드한 코드를 압축
#        run: zip -r msa_of_memory.zip ./eureka_server/cloud-discovery-eureka-server/target

      # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
      - name: Update dependency graph
        uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
